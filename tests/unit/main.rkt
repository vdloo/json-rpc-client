(require net/url)
;(require mock)

(require "../json-rpc-client/main.rkt")

;(define main-tests
;  (test-suite
;    "My example test"
;
;    (test-case
;      "test that my function is called with the right argument"
;      (let ([mock-function-i-want-to-test (make-mock a-function-i-want-to-test)]
;	    [return (a-function-i-want-to-test 1)])
;	(check-mock-called-with? '(1) mock-function-i-want-to-test)))))
